# QA 리포트 — 태스크 9: 편집 / 미리보기 + 책 뷰어 (R1)

## 전체 판정: PASS
## 가중 점수: 7.6 / 10.0

## 항목별 점수
- 기능 완성도 (30%): 8/10 — SPEC 완료 기준의 모든 기능이 구현됨. 편집 화면 (썸네일 사이드바, 텍스트 인라인 편집, 스토리/이미지 재생성, 갤러리 모달), 책 뷰어 (플립 애니메이션, 전체화면, 키보드 네비게이션, 인디케이터), 독립 뷰어 페이지 모두 동작. 다만 편집 화면에서 "주문하기"/"완료" 등 다음 단계로의 흐름 버튼이 없어 사용자가 편집 완료 후 어떻게 진행하는지 불명확.
- 코드 품질 (25%): 8/10 — 보안 취약점 없음. API Key 노출 없음. 에러 처리 적절 (403, 404, 422). generate_dummy_story에서 기존 페이지 cascade 삭제 후 재생성 정상. 코드 중복이 일부 존재(BookViewer 컴포넌트가 edit/page.tsx와 books/[id]/view/page.tsx에 거의 동일하게 중복 구현).
- API 연동 (20%): 7/10 — 6개 API 모두 정확히 구현. SPEC 대로 RESTful 엔드포인트 설계. 인증 헤더 올바르게 적용. 에러 응답 코드 적절. 다만 이 태스크는 Book Print API 연동이 직접 관련되지 않으므로 큰 감점 없음. 스토리 재생성 시 기존 페이지 이미지의 cascade 삭제가 DB 레벨에서 잘 처리됨.
- 디자인 품질 (25%): 7/10 — 파스텔 톤 색상 팔레트 일관 적용. 둥근 모서리, 부드러운 그림자. Framer Motion 애니메이션(페이지 전환, 토스트, 모달, 플립). 반응형 레이아웃(lg: 사이드바 세로, 모바일: 수평 스크롤). 다만 Phase 2 특성상 실제 이미지가 없어 placeholder만 표시되는 점은 이해하나, 책 뷰어에서 일러스트 영역이 단조로움. 모바일에서 이미지 재생성 버튼이 별도로 노출되는 처리가 잘 되어 있음.

## SPEC 완료 기준 대조
- [PASS] 완료 기준 1 (편집 화면): 페이지 목록 썸네일 사이드바(lg: 좌측 세로, 모바일: 상단 수평 스크롤), 선택된 페이지 일러스트+텍스트 좌우 배치(md:flex-row), 텍스트 인라인 편집(편집 버튼→textarea→저장/취소), 스토리 재생성(남은 횟수 표시, 최대 3회, 연쇄 처리), 이미지 재생성(남은 횟수 표시, 최대 4회, 갤러리 방식), 횟수 초과 시 비활성화+경고 메시지 — 모두 확인됨
- [PASS] 완료 기준 2 (책 뷰어): rotateY 기반 페이지 플립 애니메이션, 좌우 페이지 스프레드 표시(표지 단독, 내용 2장씩), Fullscreen API 기반 전체화면, 이전/다음 버튼+키보드 화살표/스페이스+Escape, 하단 도트 인디케이터, 독립 뷰어 페이지(/books/[id]/view) — 모두 확인됨
- [PASS] 완료 기준 3 (백엔드 API 6개): GET /pages, PATCH /pages/:pageId (텍스트 수정), POST /regenerate-story (횟수 체크+연쇄 삭제 재생성), POST /pages/:pageId/regenerate-image (갤러리 방식+횟수 체크), GET /pages/:pageId/images (갤러리 조회), PATCH /pages/:pageId/images/:imgId/select (이미지 선택) — 모두 확인됨
- [PASS] 완료 기준 4 (디자인+반응형): 파스텔 톤, 둥근 모서리, Framer Motion 애니메이션, 모바일/태블릿/데스크톱 반응형 레이아웃 — 확인됨

## 테스트 검증
- Developer 테스트 수: 18개 (test_edit_pages.py)
- 전체 테스트 157개 모두 통과 (87.02초)
- 프론트엔드 빌드 성공
- 빠진 테스트 케이스:
  - 인증 없이 편집 API 호출 시 401 반환 테스트 (다른 테스트 파일에서 동일 패턴이 확인되므로 심각하지 않음)
  - 스토리 재생성 시 기존 page_images까지 실제 cascade 삭제되는지 직접 확인하는 테스트 (test_regenerate_story_resets_image_regen_counts에서 간접 확인됨)

## 참고 사항 (다음 태스크에서 고려)
1. **BookViewer 컴포넌트 중복**: `frontend/src/app/create/edit/page.tsx`의 BookViewer와 `frontend/src/app/books/[id]/view/page.tsx`의 뷰어 로직이 거의 동일함. 태스크 14 (디자인 통합 마무리)에서 공통 컴포넌트로 추출하는 것을 권장.
2. **편집 완료 후 흐름**: 편집 화면에서 주문 단계로 이동하는 버튼이 없음. 태스크 10 (주문)에서 자연스럽게 추가될 것으로 예상.

## 구체적 개선 지시
없음. 모든 완료 기준 충족, 합격 수준 도달.
